import os
node = kwargs['node']
node.hm().loader.nodeprop(node)
###init
node.hm().loader.init_connect()
###init collect parm old struct
node.hm().loader.collectdir(node)        
node.hm().loader.dorat(node)
node.hm().loader.doaces(node)
node.hm().loader.setmenustart(node)

        
        